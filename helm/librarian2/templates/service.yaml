apiVersion: v1
kind: Service
metadata:
  name: {{ include "librarian2.fullname" . }}
  labels:
    {{- include "librarian2.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "librarian2.selectorLabels" . | nindent 4 }}
